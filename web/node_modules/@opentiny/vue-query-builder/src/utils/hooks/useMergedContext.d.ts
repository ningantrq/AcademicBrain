import type { QueryBuilderContextProps, WithRequired } from '../../ts';
export type UseMergedContextProps = WithRequired<QueryBuilderContextProps, 'translations'>;
/**
 * Inherit context, but props take precedence
 */
export declare const useMergedContext: (props: UseMergedContextProps) => any;
