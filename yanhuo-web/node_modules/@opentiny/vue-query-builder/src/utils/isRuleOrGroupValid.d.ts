import type { RuleGroupTypeAny, RuleType, RuleValidator, ValidationResult } from '../ts';
export declare const isValidationResult: (vrs?: ValidationResult) => vrs is ValidationResult;
export declare const isRuleOrGroupValid: (rg: RuleGroupTypeAny | RuleType, validationResults?: ValidationResult | boolean, validator?: RuleValidator) => boolean;
