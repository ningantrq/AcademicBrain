import type { ParseNumbersMethod } from '../ts';
export interface ParseNumberOptions {
    parseNumbers?: ParseNumbersMethod;
}
export declare const parseNumber: (v: any, { parseNumbers }: ParseNumberOptions) => any;
