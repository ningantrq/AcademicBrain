import type { RuleGroupTypeAny, RuleType } from '../ts';
export interface PreparerOptions {
    idGenerator?: () => string;
}
/**
 * Generates a valid rule
 */
export declare const prepareRule: (rule: RuleType, { idGenerator }?: PreparerOptions) => any;
/**
 * Generates a valid rule group
 */
export declare const prepareRuleGroup: <RG extends RuleGroupTypeAny>(queryObject: RG, { idGenerator }?: PreparerOptions) => RG;
/**
 * Generates a valid rule or group
 */
export declare const prepareRuleOrGroup: <RG extends RuleGroupTypeAny>(rg: RuleType | RG, { idGenerator }?: PreparerOptions) => any;
