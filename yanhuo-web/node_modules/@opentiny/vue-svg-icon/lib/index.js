import { defineComponent, $prefix } from "@opentiny/vue-common";
import { openBlock, createElementBlock, mergeProps, toHandlers } from "vue";
function _createForOfIteratorHelperLoose_tiny(o, allowArrayLike) {
  var it = typeof Symbol !== "undefined" && o[Symbol.iterator] || o["@@iterator"];
  if (it)
    return (it = it.call(o)).next.bind(it);
  if (Array.isArray(o) || (it = _unsupportedIterableToArray_tiny(o)) || allowArrayLike && o && typeof o.length === "number") {
    if (it)
      o = it;
    var i = 0;
    return function() {
      if (i >= o.length)
        return { done: true };
      return { done: false, value: o[i++] };
    };
  }
  throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
}
function _unsupportedIterableToArray_tiny(o, minLen) {
  if (!o)
    return;
  if (typeof o === "string")
    return _arrayLikeToArray_tiny(o, minLen);
  var n = Object.prototype.toString.call(o).slice(8, -1);
  if (n === "Object" && o.constructor)
    n = o.constructor.name;
  if (n === "Map" || n === "Set")
    return Array.from(o);
  if (n === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n))
    return _arrayLikeToArray_tiny(o, minLen);
}
function _arrayLikeToArray_tiny(arr, len) {
  if (len == null || len > arr.length)
    len = arr.length;
  for (var i = 0, arr2 = new Array(len); i < len; i++)
    arr2[i] = arr[i];
  return arr2;
}
var _export_sfc = function _export_sfc2(sfc, props) {
  var target = sfc.__vccOpts || sfc;
  for (var _iterator = _createForOfIteratorHelperLoose_tiny(props), _step; !(_step = _iterator()).done; ) {
    var _step$value = _step.value, key = _step$value[0], val = _step$value[1];
    target[key] = val;
  }
  return target;
};

import "@opentiny/vue-theme/svg/index.css";
var _sfc_main = defineComponent({
  name: $prefix + "SvgIcon",
  props: {
    src: {
      type: String,
      required: true
    },
    width: String,
    height: String,
    fill: String
  },
  computed: {
    styleExternalIcon: function styleExternalIcon() {
      var style = {
        mask: "url(" + this.src + ") no-repeat 50% 50%",
        "-webkit-mask": "url(" + this.src + ") no-repeat 50% 50%"
      };
      this.width && (style.width = this.width);
      this.height && (style.height = this.height);
      this.fill && (style.background = this.fill);
      return style;
    }
  }
});
function _sfc_render(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock(
    "i",
    mergeProps({
      style: _ctx.styleExternalIcon,
      class: "tiny-svg"
    }, toHandlers(_ctx.$listeners, true)),
    null,
    16
    /* FULL_PROPS */
  );
}
var SvgIcon = /* @__PURE__ */ _export_sfc(_sfc_main, [["render", _sfc_render]]);
var version = "3.13.0";
SvgIcon.install = function(Vue) {
  Vue.component(SvgIcon.name, SvgIcon);
};
SvgIcon.version = version;
export {
  SvgIcon as default
};
